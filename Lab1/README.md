# Отчет по лабораторной работе

## Студент:
ФИО: Бушмелев Константин Алексеевич \
Группа: P3318 \
Преподаватель: Клименков Сергей Викторович \
Вариант ЛР: Linux clone ema-sort-int factorizeыу

## Описание задания

### Часть 1. Запуск программ
Необходимо реализовать собственную оболочку командной строки - shell. Выбор ОС для реализации производится на усмотрение студента. Shell должен предоставлять пользователю возможность запускать программы на компьютере с переданными аргументами командной строки и после завершения программы показывать реальное время ее работы (подсчитать самостоятельно как «время завершения» – «время запуска»).

### Часть 2. Мониторинг и профилирование
Разработать комплекс программ-нагрузчиков по варианту, заданному преподавателем. Каждый нагрузчик должен, как минимум, принимать параметр, который определяет количество повторений для алгоритма, указанного в задании. Программы должны нагружать вычислительную систему, дисковую подсистему или обе подсистемы сразу. Необходимо скомпилировать их без опций оптимизации компилятора.

Перед запуском нагрузчика, попробуйте оценить время работы вашей программы или ее результаты (если по варианту вам досталось измерение чего либо). Постарайтесь обосновать свои предположения. Предположение можно сделать, основываясь на свой опыт, знания ОС и характеристики используемого аппаратного обеспечения.

- Запустите программу-нагрузчик и зафиксируйте метрики ее работы с помощью инструментов для профилирования. Сравните полученные результаты с ожидаемыми. Постарайтесь найти объяснение наблюдаемому.

- Определите количество нагрузчиков, которое эффективно нагружает все ядра процессора на вашей системе. Как распределяются времена  USER%, SYS%, WAIT%, а также реальное время выполнения нагрузчика, какое количество переключений контекста (вынужденных и невынужденных) происходит при этом?

- Увеличьте количество нагрузчиков вдвое, втрое, вчетверо. Как изменились времена, указанные на предыдущем шаге? Как ведет себя ваша система?

- Объедините программы-нагрузчики в одну, реализованную при помощи потоков выполнения, чтобы один нагрузчик эффективно нагружал все ядра вашей системы. Как изменились времена для того же объема вычислений? Запустите одну, две, три таких программы.

- Добавьте опции агрессивной оптимизации для компилятора. Как изменились времена? На сколько сократилось реальное время исполнения программы нагрузчика?

### Ограничения

- Программа (комплекс программ) должна быть реализован на языке C, C++.

- Дочерние процессы должны быть созданы через заданные системные вызовы выбранной операционной системы, с обеспечением корректного запуска и и завершения процессов.  Запрещено использовать высокоуровневые абстракции над системными вызовами. Необходимо использовать, в случае Unix, процедуры libc.

### 1. Оценка времени работы программы и ее результатов

Время работы программы зависит от размера входного массива и величины числа следующим образом: 

$𝑇_{sort}(𝑛)=C * 𝑂(𝑛log⁡𝑛)$, 

где 𝑛 — количество элементов в массиве.

$𝑇_{factorize}(𝑁)=C * 𝑂(\sqrt𝑁)$

Если грубо положить $C = 10^{-6}$; n = 100, N = 123456789, получается 

$𝑇_{sort}(100)=10^{-8} * 100 * \log{100} = 7 * 10^{-6}$

$𝑇_{factorize}(123456789) = $1.1 * 10^{-4}$

### 2. Измерения и анализ работы программы-нагрузчика

Для запуска программы-нагрузчика использовались инструменты профилирования, такие как `top`, `htop`

**Результаты профилирования:**
sort:
- **USER%**: 12.5 — Время, потраченное процессором на выполнение пользовательского кода.
- **SYS%**: 0.2 — Время, потраченное процессором на выполнение системных вызовов.
- **WAIT%**: 0.0 — Время, проведенное в ожидании, например, на ввод/вывод.
- **Реальное время**: $7 * 10^{-6}$ c — Время с начала и до окончания выполнения программы.

factorize:
- **USER%**: 12.6
- **SYS%**: 0.1
- **WAIT%**: 0.0
- **Реальное время**: $8 * 10^{-6}$

### 3. Определение оптимального количества нагрузчиков

Эффективно нагружают систему 7 нагрузчиков. 

**Результаты с 7 нагрузчиками:**
- **USER%**: 86.7
- **SYS%**: 0.1
- **WAIT%**: 0.0
- **Время работы**: $8.4 * 10^{-6}$
- **Контекстные переключения**: 151

### 4. Увеличение количества нагрузчиков

После увеличения количества нагрузчиков вчетверо были получены следующие результаты:

- **USER%**: 99.8
- **SYS%**: 0.3
- **WAIT%**: 0.0
- **Время работы**: $44.70 * 10^{-6}$
- **Контекстные переключения**: 1232

**Анализ поведения системы**:

- Когда количество нагрузчиков увеличивается до числа, равного количеству физических ядер на процессоре, каждый процесс может быть назначен на отдельное ядро, что приводит к полной загрузке процессора.

- Когда количество нагрузчиков превышает количество ядер, ОС начинает делить нагрузку между ядрами, пытаясь распределить задачи между ними. В это время несколько процессов могут одновременно выполнять свою работу на одном ядре, что приводит к дополнительным переключениям контекста.

- Это означает, что при увеличении нагрузки выше количества ядер эффективность использования процессора может снижаться из-за конкуренции за ресурсы, что приведет к увеличению времени выполнения и ухудшению производительности.

### 5. Реализация программы с потоками 

**Результаты с многопоточностью:**
- **USER%**: 100.0
- **SYS%**: 0.0
- **WAIT%**: 0.0
- **Реальное время**: $12.7 * 10^{-6}$
- **Контекстные переключения**: 255

С использованием многопоточности в положительную сторону изменилось время работы программы при неполной, но высокой загрузкой ядер. Указанные выше характеристики замерены при полной загрузке системы, в этом случае оптимизации достичь не удалось. При запуске трех таких программ время исполнения также увеличилось втрое, так как все ядра загружены даже одной такой программой.

### 6. Оптимизация компилятора

Для улучшения производительности был применен флаг агрессивной оптимизации компилятора `-O3`.

**Результаты после оптимизации:**
- **USER%**: 15.7
- **SYS%**: 0.2
- **WAIT%**: 0.0
- **Реальное время**: $0.45 * 10^{-6}$
- **Контекстные переключения**: 3

После оптимизаций компилятор время исполнения сущевственно (в 25 раз) уменьшилось. Потоки почти не загружены, небольшое количество контекстных переключений.

## Листинг исходного кода

### Программа-нагрузчик

```cpp
void sort_array(std::vector<int>& arr) {
  std::sort(arr.begin(), arr.end());
}

void factorize(int number) {
  for (int i = 2; i <= number / 2; ++i) {
    while (number % i == 0) {
      number /= i;
    }
  }
}

int load_factorize(void* args) {
  int iterations = *reinterpret_cast<int*>(args);

  int const number = 123456789;
  for (int i = 0; i < iterations; ++i) {
    factorize(number);
  }

  return 0;
}

int load_sort(void* args) {
  int iterations = *reinterpret_cast<int*>(args);
  std::vector<int> arr;
  int size = 100;
  generate_random_array(arr, size, 1, 1000);
  for (int i = 0; i < iterations; ++i) {
    sort_array(arr);
  }
  return 0;
}
```

###  Множество нагрузчиков

```cpp
std::vector<pid_t> pids;
for (int i = 0; i < 7; ++i) {
  pid_t pid;
  int* iterations_ptr = new int(iterations);
  if (args[0] == "factorize") {
    pid = clone(load_factorize, child_stack + STACK_SIZE, SIGCHLD, iterations_ptr);
  } else {
    pid = clone(load_sort, child_stack + STACK_SIZE, SIGCHLD, iterations_ptr);
  }

  if (pid == -1) {
    perror("Error creating process");
    continue;
  }
  pids.push_back(pid);
}

for (pid_t pid : pids) {
  waitpid(pid, nullptr, 0);
}
```

### Многопоточная программа

```cpp
unsigned int num_threads = std::thread::hardware_concurrency(); // Количество доступных ядер

      std::vector<std::thread> threads;
      for (unsigned int i = 0; i < num_threads; ++i) {
        if (args[0] == "factorize") {
          threads.push_back(std::thread([&]() { load_factorize(&iterations); }));
        } else {
          threads.push_back(std::thread([&]() { load_sort(&iterations); }));
        }
      }

      for (auto& th : threads) {
        th.join();
      }
```

### Вывод
В ходе выполнения лабораторной работы были достигнуты следующие результаты:

При запуске одного нагрузчика система использует лишь часть доступных ядер процессора. С увеличением количества нагрузчиков до 7, CPU было более эффективно загружено. При дальнейшем уведичении стало уже много контекстных переключений и, как следствие, больше время ожидания.

Использование многопоточности дало результаты только не при полной загрузке системы, тогда переключений контекста стало слишком много.

После применения агрессивных оптимизаций компилятора (-O3) реальное время исполнения программы сократилось в 25 раз. Компилятор эффективно (лучше меня) оптимизирует код для уменьшения времени выполнения.
